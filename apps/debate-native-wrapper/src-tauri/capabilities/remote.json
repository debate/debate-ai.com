{
  "$schema": "../gen/schemas/remote-schema.json",
  "identifier": "remote",
  "description": "Scoped IPC access for the wrapped site itself (profiles/<name>.json's trustedOrigins): opening the system browser for the Google OAuth handoff, and reading the host description its About system panel renders. Nothing else is exposed to remote content.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "https://debate-ai.com/*",
      "https://*.debate-ai.com/*"
    ]
  },
  "permissions": [
    "opener:allow-open-url",
    "allow-system-info"
  ]
}
