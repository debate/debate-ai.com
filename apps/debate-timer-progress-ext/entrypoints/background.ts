export default defineBackground(() => {
  // MV3 service worker. The popup is a standard action popup (see wxt.config.ts),
  // so there is nothing to do on icon click. Kept for future messaging / alarms.
});
