/**
 * These components render inside a Next.js app: they use styled-jsx (`<style jsx>`),
 * next/image and next/navigation. Referencing Next's ambient types here — exactly as
 * an app's generated next-env.d.ts does — keeps this package type-checkable on its own.
 */
/// <reference types="next" />
/// <reference types="next/image-types/global" />
