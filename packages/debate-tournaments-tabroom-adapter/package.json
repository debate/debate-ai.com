{
  "name": "debate-tournaments-tabroom-adapter",
  "version": "0.1.0",
  "description": "Adapter over the debate-tournaments git submodule (github.com/debate/debate-tournaments, upstream Tabroom) for the web UI: its @tabroom/types Zod schemas and inferred types, with a name-keyed registry and a non-throwing parse helper",
  "private": true,
  "type": "module",
  "author": "vtempest",
  "license": "RPL-1.5",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "postinstall": "node scripts/link-upstream-deps.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.5.4",
    "zod-openapi": "^6.0.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
