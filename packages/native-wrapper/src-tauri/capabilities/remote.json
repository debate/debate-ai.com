{
  "$schema": "../gen/schemas/remote-schema.json",
  "identifier": "remote",
  "description": "Scoped IPC access for the wrapped site itself (profiles/<name>.json's trustedOrigins), so its login page can ask the wrapper to open the system browser for Google OAuth. Nothing else is exposed to remote content.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://debate-ai.com/*", "https://*.debate-ai.com/*"]
  },
  "permissions": ["opener:allow-open-url"]
}
